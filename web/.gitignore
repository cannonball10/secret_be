node_modules/
dist/
.vite/
*.tsbuildinfo
.DS_Store

# Emitted JS from earlier tsc runs — tsconfig now has noEmit: true, but
# if anyone flips it we don't want the stray files in source control.
src/**/*.js
src/**/*.js.map
src/**/*.d.ts
